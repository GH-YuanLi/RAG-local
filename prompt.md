# 任务
最终目标：在当前路径下，搭建一个本地 RAG 系统。

# 注意

- 当前路径中的 local-rag-plan.md 文件是一个本地 RAG 系统技术方案，请基于第三部分的**需求**对该技术方案进行调整和细化；
- 技术方案调整结束后请让我确认，在确认前请不要执行任何操作；
- 之后的实施将会按照调整后的方案执行；
- 在当前路径下，创建一个log文件夹，将每一步的执行状态保存并记录


# 需求

- **开发环境**
	- 请在当前路径下，用 uv 进行项目开发和管理；
	- 开发过程的所有三方库依赖请在项目空间中安装，切勿以全局系统级别安装。

- **embedding 封装**
	- 选用本地模式；
	- 评估当前设备的配置，选择可以支持 CPU 的主流 embedding 模型。
	
- **LLM 调用方式**
	- 选择远程API调用；
	- API调用配置需要兼容 minimax 和 glm 等主流大模型厂商的接口。
	
- **RAG 架构选择**
	- 分析主流 RAG 架构的优略势；
	- 基于以上需求和当前设备配置选择最合适的架构。
	
- **最终实现的效果**
	- 支持上传多个文件；
	- 用户可以直接通过 Web 可视化界面，完成从文档上传、查询到结果输出的全部流程；
	- 其他功能：
		- 功能1：在可视化界面中，支持配置不同的LLM API接口信息，用来切换大模型；
	- 创建 README.md 文档，指导用户如何使用该RAG系统。